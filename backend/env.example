# Miljökonfiguration för Tetris-backend
# Kopiera denna fil till .env och fyll i dina värden

# --- Serverkonfiguration ---
NODE_ENV=development
PORT=3001

# --- MongoDB-konfiguration ---
# URI till din MongoDB-instans (ändra vid behov)
MONGODB_URI=mongodb://localhost:27017/tetris-game

# --- CORS-konfiguration ---
# Lista med tillåtna origins separerade med kommatecken
CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# --- Säkerhetsinställningar ---
# Fönster för rate limiting (ms) och max antal requests i fönstret
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
# Begränsning för poänginskick (fönster/ms och max requests)
SCORE_LIMIT_WINDOW_MS=60000
SCORE_LIMIT_MAX_REQUESTS=10

# --- Anti-cheat inställningar ---
# Hur länge en game seed är giltig (ms)
GAME_SEED_EXPIRY_MS=300000
# Tillåten avvikelse i procent mellan rapporterad och kalkylerad poäng
SCORE_TOLERANCE_PERCENT=30

# --- Loggning ---
# Ex: debug, info, warn, error
LOG_LEVEL=info

# Valfritt: JWT-secret (om autentisering implementeras senare)
# JWT_SECRET=your-super-secret-jwt-key

# Valfritt: Admin API-nyckel (för admin-endpoints)
# ADMIN_API_KEY=your-admin-api-key
